{
    "builds": [{
        "src": "ecomm_config/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.13" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "ecomm_config/wsgi.py"
        }
    ]
}